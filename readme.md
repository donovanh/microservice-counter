# Example counter as a microservice

Based on [this article](https://mxstbr.blog/2017/01/your-first-node-microservice/).

Run using `micro index.js`.

(If `micro` not available, install using `npm install micro -g`).